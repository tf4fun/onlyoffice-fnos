<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OnlyOffice Connector 设置</title>
    <link rel="stylesheet" href="/static/bulma.min.css">
    <script src="/static/htmx.min.js"></script>
</head>
<body>
    <section class="hero is-fullheight has-background-light">
        <div class="hero-body">
            <div class="container">
                <div class="columns is-centered">
                    <div class="column is-half-desktop is-two-thirds-tablet">
                        <div class="box">
                            <h1 class="title is-4 has-text-centered">OnlyOffice Connector 设置</h1>
                            
                            <form hx-post="/api/settings" hx-target="#message" hx-swap="innerHTML">
                                <div class="field">
                                    <label class="label" for="documentServerUrl">Document Server 地址</label>
                                    <div class="field has-addons">
                                        <div class="control is-expanded">
                                            <input type="url" id="documentServerUrl" name="documentServerUrl" class="input" 
                                                   placeholder="http://192.168.1.100:8080" value="{{.Settings.DocumentServerURL}}" required>
                                        </div>
                                        <div class="control">
                                            <button type="button" class="button"
                                                    hx-post="/api/settings/validate"
                                                    hx-include="#documentServerUrl"
                                                    hx-target="#status"
                                                    hx-swap="innerHTML">
                                                测试连接
                                            </button>
                                        </div>
                                    </div>
                                    <p class="help">输入 OnlyOffice Document Server 的完整地址</p>
                                    <div id="status" class="mt-2"></div>
                                </div>

                                <div class="field">
                                    <label class="label" for="secret">JWT 密钥</label>
                                    <div class="field has-addons">
                                        <div class="control is-expanded">
                                            <input type="text" id="secret" name="documentServerSecret" class="input"
                                                   placeholder="输入或生成 JWT 密钥" value="{{.Settings.DocumentServerSecret}}">
                                        </div>
                                        <div class="control">
                                            <button type="button" class="button"
                                                    hx-post="/api/settings/generate-key"
                                                    hx-target="#secret"
                                                    hx-swap="outerHTML">
                                                重新生成
                                            </button>
                                        </div>
                                    </div>
                                    <p class="help">用于与 Document Server 安全通信的密钥，需与 Document Server 配置一致</p>
                                </div>

                                <div class="field">
                                    <label class="label" for="baseUrl">本机回调地址</label>
                                    <div class="control">
                                        <input type="url" id="baseUrl" name="baseUrl" class="input" 
                                               placeholder="http://192.168.1.100:10099" value="{{.Settings.BaseURL}}" required>
                                    </div>
                                    <p class="help">Document Server 回调本服务的地址，需填写 NAS 的内网 IP（Docker 中的 Document Server 无法访问 localhost）</p>
                                </div>

                                <div class="field mt-5">
                                    <div class="control">
                                        <button type="submit" class="button is-primary is-fullwidth">保存设置</button>
                                    </div>
                                </div>
                                <div id="message" class="mt-4"></div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>
</html>
