#!/bin/bash
# Post-installation script
# Create default config if not exists
if [ ! -f "${TRIM_PKGVAR}/config.json" ]; then
    echo '{"documentServerUrl":"","documentServerSecret":"","baseUrl":""}' > "${TRIM_PKGVAR}/config.json"
fi
exit 0
